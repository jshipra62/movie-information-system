<%= form_with(model: theater, local: true, :html => {:class => "card p-3 bg-light mx-auto", :style => "width: 35rem"}) do |form| %>
  <% if theater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(theater.errors.count, "error") %> prohibited this theater from being saved:</h2>

      <ul>
      <% theater.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group m-2">
    <%= form.text_field :name, placeholder:"Enter Theater Name:", class:'form-control' %>
  </div>

  <div class="form-group m-2">   
    <%= form.number_field :seats, placeholder:"Enter Number of Seats:", class:'form-control' %>
  </div>

  <div class="form-group m-2">
    <%= form.number_field :price, placeholder:"Enter Ticket Price:", class:'form-control' %>
  </div>

  <div class="actions m-2">
    <%= form.submit "Create Theater", :class => "btn btn-primary" %>
  </div>
<% end %>
