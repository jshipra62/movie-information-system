<%= form_with(model: language, local: true, :html => {:class => "card p-3 bg-light mx-auto", :style => "width: 35rem"}) do |form| %>
  <% if language.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(language.errors.count, "error") %> prohibited this language from being saved:</h2>

      <ul>
      <% language.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group m-2">
    <%= form.text_field :language, placeholder:"Enter Language:", class:'form-control' %>
  </div>

  <div class="actions m-2">
    <%= form.submit "Create Language", :class => "btn btn-primary" %>
  </div>
<% end %>
