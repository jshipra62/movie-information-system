<div class="reviews card px-3 pt-2 mx-auto bg-light" style="width: 30rem">
  <div class="reviews card-body">
    <p>
      <strong>Name:</strong>
      <%= @user.name %>
    </p>

    <p>
      <strong>Email:</strong>
      <%= @user.email %>
    </p>

    <% if current_user %>
      <% if current_user.id == @user.id || current_user.admin %>  
        <%= link_to 'Edit Profile', edit_user_path(@user),  class: "btn btn-danger mr-3" %> 
        <%= link_to 'Delete Account', @user, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger " %></td>
      <% else %>
        <% controller.redirect_to movies_path %>  
      <% end %>
    <% end %>
  </div>
</div>

<% if current_user %>
  <% if current_user.admin %>
    <%= link_to 'Back', users_path %>
  <% else %>
    <%= link_to 'Back', movies_path %>
  <% end %>
<% end %>

